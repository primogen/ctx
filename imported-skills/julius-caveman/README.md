# Julius Caveman

Curated mirror of [JuliusBrussee/caveman](https://github.com/JuliusBrussee/caveman),
pinned at `63a91ecadbf4`.

ctx imports the canonical root-level `skills/` and `agents/` content, not the
duplicated plugin/agent-specific copies under `.agents/`, `.junie/`, `.kiro/`,
`.roo/`, or `plugins/`.

Planned ctx entities:

- skills: `cavecrew`, `caveman`, `caveman-commit`, `caveman-compress`,
  `caveman-help`, `caveman-review`, `caveman-stats`
- agents: `cavecrew-builder`, `cavecrew-investigator`, `cavecrew-reviewer`

Run `python imported-skills/julius-caveman/build_manifest.py` after edits.
