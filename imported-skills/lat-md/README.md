# lat.md Knowledge-Graph Import

lat.md models codebase knowledge as linked markdown under `lat.md/`, with
wiki links between concepts and source-code backlinks to keep documentation
enforced.

ctx benefits from this pattern when recommending:

- durable context capture after agent work,
- markdown graph organization for large repos,
- code-reference drift checks,
- custom harness memory contracts.

This import is metadata-only and synthesized into a ctx skill.
